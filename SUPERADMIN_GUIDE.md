# 👑 Super Admin System - ระบบซุปเปอร์แอดมิน

## 📋 ภาพรวม

ระบบซุปเปอร์แอดมิน (Super Admin) คือระดับผู้ดูแลระบบสูงสุดที่มีสิทธิ์จัดการ:
- ✅ จัดการแอดมิน (สร้าง, แก้ไข, ลบ)
- ✅ จัดการผู้ใช้ทั้งระบบ
- ✅ ดูบันทึกการตรวจสอบและการอนุมัติ
- ✅ ดูสถิติของระบบทั้งหมด
- ✅ จัดการคำขอเกียรติบัตร

---

## 🚀 การติดตั้ง

### 1️⃣ สร้างบัญชี Super Admin

#### วิธีที่ 1: ใช้ Node.js Script (แนะนำ)

```bash
# ไม่มีอาร์กิวเมนต์ - ส่วนป้อนข้อมูลแบบอินเทอร์แอกทีฟ
node scripts/create-superadmin.js

# มีอาร์กิวเมนต์ - สร้างโดยตรง
node scripts/create-superadmin.js superadmin@example.com MyPassword123 "Super Administrator"
```

**ตัวอย่าง:**
```bash
node scripts/create-superadmin.js admin@kkn2.com SecurePassword123 "คุณสมนาซุปเปอร์แอดมิน"
```

**ผลลัพธ์:**
```
✅ Super Admin account created successfully!

========== SUPER ADMIN CREDENTIALS ==========
📧 Email: admin@kkn2.com
🔐 Password: SecurePassword123
👤 Name: คุณสมนาซุปเปอร์แอดมิน
👑 Role: Super Admin
==========================================

🔗 Login URL: http://your-domain/login

⚠️  IMPORTANT:
  1. Save these credentials securely
  2. Change the password after first login
  3. Share credentials only with authorized personnel
```

#### วิธีที่ 2: ใช้ SQL Command (MySQL)

```sql
-- แปลง admin ที่มีอยู่เป็น super_admin
UPDATE users SET role = 'super_admin' WHERE id = 1;

-- หรือสร้างใหม่โดยตรง
INSERT INTO users (full_name, email, password, role, created_at, updated_at)
VALUES ('Super Administrator', 'superadmin@kkn2.com', '$2b$10$...hashed_password...', 'super_admin', NOW(), NOW());
```

---

## 🔐 ลงชื่อเข้าระบบ

### Login Page
```
URL: http://your-domain/login
Email: admin@kkn2.com
Password: MySecurePassword123
```

เมื่อล็อกอินสำเร็จ ระบบจะเปลี่ยนเส้นทางไปยัง **แดชบอร์ด Super Admin**

---

## 📊 แดชบอร์ด Super Admin

### URL: `/superadmin/dashboard`

#### ส่วนประกอบหลัก:

**1. 📈 สถิติระบบ (Statistics Cards)**
- 👤 จำนวนแอดมิน
- 👑 จำนวน Super Admin
- 🎓 จำนวนครู
- 👨‍💼 จำนวนบุคลากร
- 📋 จำนวนคำขอเกียรติบัตร
- ✅ อนุมัติ
- ⏳ รอตรวจสอบ
- ❌ ไม่อนุมัติ

**2. 👥 ตารางแอดมินล่าสุด**
- แสดง 10 แอดมินล่าสุด
- ปุ่มเข้าถึงด่วน "จัดการทั้งหมด"

**3. 📄 คำขอเกียรติบัตรล่าสุด**
- แสดง 5 คำขอล่าสุด
- สถานะของแต่ละคำขอ
- วันที่และข้อมูลผู้ขอ

**4. 🔗 ลิงก์ด่วน (Quick Links)**
- 📋 จัดการแอดมิน
- 👥 จัดการผู้ใช้
- 📊 บันทึกการตรวจสอบ

---

## 👥 จัดการแอดมิน

### URL: `/superadmin/admins`

#### ฟีเจอร์หลัก:

#### 1. 📋 รายการแอดมิน
```
- #: เลขลำดับ
- ชื่อเต็ม: ชื่อของแอดมิน
- อีเมล: อีเมลสำหรับล็อกอิน
- โทรศัพท์: เบอร์โทรศัพท์
- โรงเรียน: โรงเรียนที่สังกัด
- บทบาท: Admin หรือ Super Admin
- สร้างเมื่อ: วันที่สร้างบัญชี
- การจัดการ: ปุ่มแก้ไขและลบ
```

#### 2. ➕ เพิ่มแอดมินใหม่
**ฟอร์มเพิ่มแอดมิน:**
```
- ชื่อเต็ม (required)
- อีเมล (required) - ต้องไม่ซ้ำ
- โทรศัพท์ (optional)
- โรงเรียน (optional)
- บทบาท (required) - Admin หรือ Super Admin
- รหัสผ่าน (required) - ต้องยาวกว่า 8 ตัวอักษร
- ยืนยันรหัสผ่าน (required) - ต้องตรงกันกับรหัสผ่าน
```

**ตัวอย่างการสร้างแอดมิน:**
```
ชื่อเต็ม: นาย สมชาย ศรีวัฒนา
อีเมล: somchai@kkn2.com
โทรศัพท์: 0912345678
โรงเรียน: โรงเรียน...
บทบาท: Admin
รหัสผ่าน: SecurePass123
ยืนยันรหัสผ่าน: SecurePass123
```

#### 3. ✏️ แก้ไขแอดมิน

**URL:** `/superadmin/admins/{id}/edit`

**ฟีเจอร์:**
- แก้ไขข้อมูลทั้งหมด
- เปลี่ยนรหัสผ่าน (optional)
- เปลี่ยนบทบาท (Admin ↔ Super Admin)

**หมายเหตุ:** 
- ปล่อยช่องรหัสผ่านว่างเพื่อใช้รหัสผ่านเดิม
- ไม่สามารถลบตัวเอง (ป้องกันการล็อคเกอร์)

#### 4. 🗑️ ลบแอดมิน

**URL:** `/superadmin/admins/{id}/delete` (POST)

**ป้องกัน:**
- ไม่สามารถลบตัวเอง
- ต้องยืนยันการลบ

---

## 👫 จัดการผู้ใช้ระบบ

### URL: `/superadmin/users`

#### ฟีเจอร์:

**1. 🔍 ตัวกรองและค้นหา**
```
- ตัวเลือกบทบาท:
  - ทั้งหมด
  - ครู
  - บุคลากร
  - แอดมิน
  - Super Admin
- ช่องค้นหา: ชื่อ, อีเมล, หรือโรงเรียน
```

**2. 📊 ตารางผู้ใช้**
```
- #: เลขลำดับ
- ชื่อเต็ม: ชื่อของผู้ใช้
- อีเมล: อีเมลสำหรับล็อกอิน
- โรงเรียน: โรงเรียนที่สังกัด
- บทบาท: บทบาทของผู้ใช้
- สร้างเมื่อ: วันที่สร้างบัญชี
- การจัดการ: ปุ่มลบ
```

**3. 🗑️ ลบผู้ใช้**
- ยืนยันการลบ
- ไม่สามารถลบตัวเอง

---

## 📊 บันทึกการตรวจสอบ (Audits)

### URL: `/superadmin/audits`

#### ข้อมูลที่แสดง:

```
- โครงการ/กิจกรรม: ชื่อของโครงการ
- ผู้ขอ: ชื่อผู้ขอเกียรติบัตร
- สถานะ: ✅ อนุมัติ หรือ ❌ ไม่อนุมัติ
- ผู้จัดการ: ชื่อของแอดมินที่ทำการอนุมัติ/ปฏิเสธ
- วันที่: วันที่ทำการอนุมัติ/ปฏิเสธ
- หมายเหตุ: เหตุผลการปฏิเสธ (ถ้ามี)
```

#### ใช้งาน:
- ติดตามการตรวจสอบของแต่ละแอดมิน
- ตรวจสอบประวัติการอนุมัติ/ปฏิเสธ
- ดูเหตุผลที่ปฏิเสธ

---

## 🔒 ความปลอดภัยและสิทธิ์

### สิทธิ์ของ Super Admin:
- ✅ เข้าถึง `/superadmin/*` ทุกหน้า
- ✅ สร้าง, แก้ไข, ลบแอดมิน
- ✅ สร้าง, แก้ไข, ลบผู้ใช้
- ✅ ดูบันทึกการตรวจสอบ
- ✅ ดูสถิติระบบ

### สิทธิ์ของ Admin:
- ✅ เข้าถึง `/admin/*` ทั่วไป
- ✅ อนุมัติ/ปฏิเสธคำขอ
- ✅ จัดการครูและข้อมูล
- ❌ ไม่สามารถเข้า `/superadmin/*`
- ❌ ไม่สามารถจัดการแอดมิน

### การตรวจสอบสิทธิ์:
```javascript
// Middleware ตรวจสอบ Super Admin
function requireSuperAdmin(req, res, next) {
  if (!req.session.userId) {
    return res.redirect('/login');
  }
  
  // ตรวจสอบจากฐานข้อมูล
  db.execute('SELECT * FROM users WHERE id = ? AND role = ?', 
             [req.session.userId, 'super_admin'])
    .then(([rows]) => {
      if (!rows[0]) {
        return res.status(403).render('error', { 
          message: 'ไม่มีสิทธิ์เข้าถึง'
        });
      }
      next();
    });
}
```

---

## 🛠️ การแก้ไขปัญหาทั่วไป

### ❌ "ไม่พบแอดมิน"
**สาเหตุ:** ไม่มี Super Admin ในระบบ
**วิธีแก้:**
```bash
node scripts/create-superadmin.js
```

### ❌ "ไม่มีสิทธิ์เข้าถึง"
**สาเหตุ:** บัญชีผู้ใช้ไม่ได้เป็น Super Admin
**วิธีแก้:**
```sql
UPDATE users SET role = 'super_admin' WHERE email = 'your-email@example.com';
```

### ❌ "อีเมลนี้มีการใช้งานแล้ว"
**สาเหตุ:** อีเมลซ้ำในระบบ
**วิธีแก้:**
- ใช้อีเมลอื่น
- หรือแก้ไขอีเมลของแอดมินที่มีอยู่

### ❌ "รหัสผ่านไม่ตรงกัน"
**สาเหตุ:** การยืนยันรหัสผ่านไม่ตรงกัน
**วิธีแก้:**
- ตรวจสอบว่ากรอกรหัสผ่าน 2 ครั้งเหมือนกัน

### ❌ "ไม่สามารถลบตัวเองได้"
**สาเหตุ:** ป้องกันการล็อคเกอร์ของระบบ
**วิธีแก้:**
- ให้ Super Admin คนอื่นลบได้
- หรือใช้ SQL `DELETE` โดยตรง

---

## 📝 SQL Queries สำหรับการจัดการ

### ดู Super Admin ทั้งหมด
```sql
SELECT id, full_name, email, role, created_at 
FROM users 
WHERE role = 'super_admin';
```

### ดู Admin ทั้งหมด
```sql
SELECT id, full_name, email, role, created_at 
FROM users 
WHERE role IN ('admin', 'super_admin');
```

### เปลี่ยน Admin เป็น Super Admin
```sql
UPDATE users SET role = 'super_admin' WHERE id = 5;
```

### เปลี่ยน Super Admin เป็น Admin
```sql
UPDATE users SET role = 'admin' WHERE id = 5 AND id != 1;
-- (ป้องกันการลบ Super Admin ตัวสุดท้าย)
```

### ลบแอดมิน
```sql
DELETE FROM users WHERE id = 5 AND role IN ('admin', 'super_admin');
```

---

## 📚 ไฟล์ที่เกี่ยวข้อง

```
project/
├── routes/
│   └── superadmin.js                    # Super Admin Routes
├── views/admin/
│   ├── superadmin-dashboard.ejs         # แดชบอร์ด Super Admin
│   ├── superadmin-admins.ejs            # จัดการแอดมิน
│   ├── superadmin-admin-edit.ejs        # แก้ไขแอดมิน
│   ├── superadmin-users.ejs             # จัดการผู้ใช้
│   └── superadmin-audits.ejs            # บันทึกการตรวจสอบ
├── scripts/
│   └── create-superadmin.js             # Script สร้าง Super Admin
├── migrations/
│   └── 03-superadmin-setup.sql          # SQL Setup Guide
└── app.js                               # ลงทะเบียน routes
```

---

## 🌐 URL Reference

```
/superadmin/dashboard              # แดชบอร์ด Super Admin
/superadmin/admins                 # จัดการแอดมิน (GET)
/superadmin/admins/create          # สร้างแอดมิน (POST)
/superadmin/admins/{id}/edit       # แก้ไขแอดมิน (GET)
/superadmin/admins/{id}/update     # บันทึกแก้ไข (POST)
/superadmin/admins/{id}/delete     # ลบแอดมิน (POST)
/superadmin/users                  # จัดการผู้ใช้ (GET)
/superadmin/users/{id}/delete      # ลบผู้ใช้ (POST)
/superadmin/audits                 # บันทึกการตรวจสอบ (GET)
```

---

## 💡 Best Practices

### ✅ ทำ:
- ✅ เปลี่ยนรหัสผ่านหลังจากสร้างบัญชี Super Admin
- ✅ เก็บสิทธิ์ Super Admin ให้อยู่ในมือคนน้อย (1-2 คน)
- ✅ ติดตามบันทึกการตรวจสอบอย่างสม่ำเสมอ
- ✅ ใช้รหัสผ่านที่แข็งแรง (ความยาว 8+ ตัวอักษร, ตัวพิมพ์, ตัวเลข, สัญลักษณ์)
- ✅ ลบแอดมินที่ไม่ใช้งานแล้ว

### ❌ อย่าทำ:
- ❌ แชร์รหัสสัญญาณ Super Admin
- ❌ ให้สิทธิ์ Super Admin แก่ผู้ใช้ที่ไม่น่าเชื่อถือ
- ❌ ตั้งรหัสผ่านที่อ่อนแอ
- ❌ ลบ Super Admin คนสุดท้ายออกจากระบบ
- ❌ ไม่ตรวจสอบหรือทำให้เป็นปัจจุบันบันทึก

---

## 📞 การติดต่อสำหรับปัญหา

หากพบปัญหา:
1. ตรวจสอบบันทึกข้อผิดพลาด (logs)
2. ทำให้มั่นใจว่าฐานข้อมูลกำลังทำงาน
3. ตรวจสอบความสิทธิ์ของฐานข้อมูล
4. ติดต่อทีมพัฒนา

---

**เวอร์ชัน:** 1.0  
**วันที่อัปเดต:** 18 กุมภาพันธ์ 2569  
**สถานะ:** ✅ ใช้งานได้
