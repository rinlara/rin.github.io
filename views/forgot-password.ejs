<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ลืมรหัสผ่าน — สพป.ขอนแก่น เขต 2</title>

  <!-- Removed Google Fonts to avoid CSP/font loading issues -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      background: linear-gradient(140deg, #e8f1ff 0%, #f6f9ff 50%, #ffffff 100%);
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .container {
      display: flex;
      max-width: 860px;
      width: 100%;
      background: #fff;
      border-radius: 26px;
      overflow: hidden;
      box-shadow: 0 18px 60px rgba(0, 60, 140, 0.15);
      animation: fadeUp .7s ease;
    }

    @keyframes fadeUp {
      0% { opacity: 0; transform: translateY(20px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    /* ===== Left Panel ===== */
    .left-panel {
      flex: 1;
      background: linear-gradient(135deg, #063f88, #1b66b4);
      color: white;
      padding: 2.8rem 1.6rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .left-panel img {
  width: 130px;
  height: 130px;         /* ให้เป็นสี่เหลี่ยมจัตุรัสก่อน แล้วค่อยทำวงกลม */
  margin-bottom: 1.2rem;
  background: white;
  padding: 0px;          /* ระยะห่างรูปกับกรอบวงกลม */
  border-radius: 50%;    /* ทำให้เป็นวงกลม */
  box-shadow: 0 6px 20px rgba(0,0,0,0.22);
  object-fit: cover;     /* รูปเต็มวงกลม ไม่ยืด ไม่เบี้ยว */
}


    .left-panel h2 {
      font-size: 1.45rem;
      font-weight: 700;
      line-height: 1.5;
      letter-spacing: .3px;
    }

    /* ===== Right Panel (Form) ===== */
    .right-panel {
      flex: 1.2;
      padding: 3rem 2.5rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    h1 {
      text-align: center;
      font-size: 1.9rem;
      font-weight: 700;
      color: #0d4a8a;
      margin-bottom: 1.5rem;
    }

    .info-text {
      color: #4a5568;
      margin-bottom: 2rem;
      line-height: 1.7;
      font-size: 1rem;
      padding: .8rem;
      background: #f1f7ff;
      border-left: 4px solid #1b66b4;
      border-radius: 10px;
    }

    label {
      font-weight: 600;
      color: #2d3748;
      display: block;
      margin-bottom: .5rem;
    }

    .form-control {
      padding: .95rem 1rem;
      border: 1px solid #cbd5e0;
      border-radius: 14px;
      font-size: 1rem;
      background: #f7faff;
      width: 100%;
      transition: .3s;
      margin-bottom: 1.6rem;
    }

    .form-control:focus {
      border-color: #1b66b4;
      background: white;
      box-shadow: 0 0 0 4px rgba(27, 102, 180, 0.15);
      outline: none;
    }

    .alert {
      padding: 1rem 1.2rem;
      border-radius: 14px;
      font-weight: 600;
      text-align: center;
      margin-bottom: 1.8rem;
      display: flex;
      justify-content: center;
      gap: .6rem;
      font-size: 1rem;
    }

    .alert-success {
      background: #f3fdf7;
      color: #207245;
      border-left: 6px solid #207245;
    }

    .alert-danger {
      background: #fff5f5;
      color: #cc2525;
      border-left: 6px solid #cc2525;
    }

    .btn-primary {
      background: linear-gradient(100deg, #0c4b89, #1b66b4);
      border: none;
      padding: 1rem;
      border-radius: 14px;
      color: white;
      font-size: 1.15rem;
      font-weight: 700;
      cursor: pointer;
      width: 100%;
      transition: .3s;
      letter-spacing: .3px;
      display: flex;
      justify-content: center;
      gap: .6rem;
    }

    .btn-primary:hover {
      background: linear-gradient(100deg, #145da3, #1f74c7);
      transform: translateY(-2px);
      box-shadow: 0 8px 18px rgba(27,102,180,0.35);
    }

    @media (max-width: 768px) {
      .container { flex-direction: column; }
      .left-panel img { max-width: 100px; }
      h1 { font-size: 1.6rem; }
      .right-panel { padding: 2.2rem 1.6rem; }
    }
  </style>
</head>

<body>
  <div class="container">

    <!-- Left Side -->
    <div class="left-panel">
      <img src="/images/logo องค์กรคุณภาพเเละธรรมาภิบาล.png" alt="โลโก้ สพฐ.">
      <h2>
        สำนักงานเขตพื้นที่การศึกษา<br>
        ประถมศึกษาขอนแก่น เขต 2
      </h2>
    </div>

    <!-- Right Side (Form) -->
    <div class="right-panel">
      <h1><i class="fas fa-key"></i> ลืมรหัสผ่าน</h1>

      <% if (locals.success) { %>
        <div class="alert alert-success">
          <i class="fas fa-check-circle"></i> <%- success %>
        </div>
      <% } %>

      <% if (locals.error) { %>
        <div class="alert alert-danger">
          <i class="fas fa-exclamation-circle"></i> <%- error %>
        </div>
      <% } %>

      <p class="info-text">
        📧 กรุณากรอกอีเมลที่คุณใช้ลงทะเบียนในระบบ  
        หากมีอีเมลนี้อยู่ ระบบจะส่งลิงก์รีเซ็ตรหัสผ่านให้ทันที
      </p>

      <form method="POST">
        <label for="email">อีเมล</label>
        <input 
          type="email" 
          id="email"
          name="email"
          class="form-control"
          placeholder="example@kk2.go.th"
          required
        >

        <button type="submit" class="btn-primary">
          <i class="fas fa-paper-plane"></i> ส่งลิงก์รีเซ็ตรหัสผ่าน
        </button>
      </form>

    </div>
  </div>
</body>
</html>
