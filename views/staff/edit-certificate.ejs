<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>แก้ไขเกียรติบัตร — สพป.ขอนแก่น เขต 2</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.5.0/css/all.min.css">
  <!-- Removed Google Fonts to avoid CSP/font loading issues -->
  <style> body { font-family: 'Sarabun', sans-serif; } </style>
</head>
<body>
  <div class="container my-5">
    <div class="card shadow">
      <div class="card-header bg-success text-white">
        <h4><i class="fas fa-edit me-2"></i> แก้ไขข้อมูลเกียรติบัตร</h4>
      </div>
      <div class="card-body">
        <% if (locals.error) { %>
          <div class="alert alert-danger alert-dismissible fade show">
            <%= error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% } %>

        <!-- ✅ แก้ action ให้ตรงกับ route POST ที่มีจริง -->
        <form action="/staff/certificates/edit/<%= certificate.id %>" method="POST" enctype="multipart/form-data">
          <div class="mb-3">
            <label class="form-label">ชื่อเกียรติบัตร <span class="text-danger">*</span></label>
            <input type="text" name="title" class="form-control" value="<%= certificate.title || '' %>" required>
          </div>

          <div class="mb-3">
            <label class="form-label">หน่วยงานผู้มอบ <span class="text-danger">*</span></label>
            <input type="text" name="issuing_agency" class="form-control" value="<%= certificate.issuing_agency || '' %>" required>
          </div>

          <div class="mb-3">
            <label class="form-label">เลขที่เกียรติบัตร</label>
            <input type="text" name="certificate_number" class="form-control" value="<%= certificate.certificate_number || '' %>">
          </div>

          <div class="mb-3">
            <label class="form-label">วันที่ออก</label>
            <input type="date" name="issue_date" class="form-control" 
                   value="<%= certificate.issue_date && certificate.issue_date !== '0000-00-00' && certificate.issue_date ? certificate.issue_date : '' %>">
          </div>

          <div class="mb-3">
            <label class="form-label">อัปโหลดไฟล์ใหม่ (ไม่บังคับ)</label>
            <input type="file" name="certificate" class="form-control" accept=".pdf,.jpg,.jpeg,.png">
            <% if (certificate.file_path) { %>
              <div class="form-text">
                ไฟล์ปัจจุบัน: 
                <!-- ✅ ลบ baseUrl ออก — ใช้ path ตรง -->
                <a href="<%= certificate.file_path %>" target="_blank" class="text-primary">ดูไฟล์</a>
              </div>
            <% } %>
          </div>

          <div class="d-flex gap-2">
            <button type="submit" class="btn btn-success"><i class="fas fa-save me-1"></i> บันทึก</button>
            <a href="/staff/certificates" class="btn btn-secondary">ยกเลิก</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>