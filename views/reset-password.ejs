<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ตั้งรหัสผ่านใหม่ — สพป.ขอนแก่น เขต 2</title>
  <!-- ✅ Google Font: Sarabun (ฟอนต์ราชการไทย) -->
  <!-- Removed external Google Fonts to comply with CSP -->
  <!-- ✅ Font Awesome จาก jsDelivr (CSP-safe) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.5.0/css/all.min.css">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: 'Sarabun', sans-serif;
      background: linear-gradient(135deg, #eaf4ff 0%, #f3f8ff 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
      color: #1e293b;
    }
    .card {
      width: 100%;
      max-width: 540px;
      background: white;
      border-radius: 24px;
      box-shadow: 0 12px 40px rgba(0, 48, 143, 0.18);
      overflow: hidden;
      animation: fadeInUp 0.5s ease;
    }

    /* หัวด้านบน — โลโก้ + ชื่อหน่วยงาน */
    .logo-section {
      text-align: center;
      padding: 2rem 1.5rem 1.5rem;
      background: linear-gradient(135deg, #00308f 0%, #0053c7 100%);
      color: white;
      position: relative;
    }
    .logo-section::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 6px;
      background: #ffd700;
      border-radius: 3px;
    }
    .logo-img {
      width: 80px;
      height: 80px;
      object-fit: contain;
      margin-bottom: 1rem;
      border-radius: 50%;
      border: 3px solid rgba(255, 255, 255, 0.3);
      background: white;
      padding: 4px;
    }
    .school-name {
      font-size: 1.35rem;
      font-weight: 700;
      line-height: 1.4;
      letter-spacing: 0.5px;
    }

    /* ส่วนหัวฟอร์ม */
    .card-header {
      text-align: center;
      padding: 1.8rem 1.5rem 1.5rem;
      background: white;
    }
    .card-header i {
      font-size: 2.4rem;
      color: #00308f;
      margin-bottom: 0.8rem;
      background: rgba(0, 48, 143, 0.08);
      width: 70px;
      height: 70px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
    }
    .card-header h2 {
      font-size: 1.8rem;
      font-weight: 700;
      color: #00308f;
      letter-spacing: 0.6px;
    }

    /* ส่วนฟอร์ม */
    .card-body {
      padding: 2rem 2rem 2.5rem;
    }
    .alert {
      padding: 0.9rem 1.2rem;
      border-radius: 12px;
      font-weight: 600;
      margin-bottom: 1.6rem;
      display: flex;
      align-items: center;
      gap: 0.8rem;
      font-size: 0.98rem;
    }
    .alert i { font-size: 1.2rem; }
    .alert-success {
      background: #f0fdf4;
      color: #166534;
      border-left: 4px solid #166534;
    }
    .alert-danger {
      background: #fef2f2;
      color: #b91c1c;
      border-left: 4px solid #b91c1c;
    }
    .form-group {
      margin-bottom: 1.6rem;
    }
    label {
      font-weight: 600;
      color: #1e293b;
      margin-bottom: 0.6rem;
      display: block;
      font-size: 1.02rem;
    }
    input {
      width: 100%;
      padding: 0.9rem 1.1rem;
      border: 1px solid #cbd5e1;
      border-radius: 12px;
      font-size: 1.02rem;
      background: #f8fafc;
      transition: all 0.3s;
    }
    input:focus {
      outline: none;
      border-color: #00308f;
      background: white;
      box-shadow: 0 0 0 3px rgba(0, 48, 143, 0.15);
    }
    .btn-primary {
      width: 100%;
      padding: 1.1rem;
      background: linear-gradient(100deg, #00308f, #0053c7);
      color: white;
      border: none;
      border-radius: 12px;
      font-size: 1.15rem;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.35s;
      letter-spacing: 0.5px;
      box-shadow: 0 4px 14px rgba(0, 48, 143, 0.25);
    }
    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 22px rgba(0, 48, 143, 0.35);
      background: linear-gradient(100deg, #00297a, #004cb5);
    }
    .back-link {
      text-align: center;
      margin-top: 1.8rem;
    }
    .back-link a {
      color: #00308f;
      font-weight: 600;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 1.02rem;
    }
    .back-link a:hover {
      text-decoration: underline;
    }

    /* Animation */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 576px) {
      .card {
        border-radius: 20px;
      }
      .logo-img {
        width: 70px;
        height: 70px;
      }
      .school-name {
        font-size: 1.15rem;
      }
      .card-header h2 {
        font-size: 1.6rem;
      }
      .btn-primary {
        font-size: 1.1rem;
      }
    }
  </style>
</head>
<body>
  <div class="card">
    <!-- โลโก้ + ชื่อหน่วยงาน -->
    <div class="logo-section">
      <img src="/images/logo องค์กรคุณภาพเเละธรรมาภิบาล.png" alt="สำนักงานเขตพื้นที่การศึกษาขอนแก่น เขต 2" class="logo-img">
      <div class="school-name">สำนักงานเขตพื้นที่การศึกษา<br>ประถมศึกษาขอนแก่น เขต 2</div>
    </div>

    <!-- หัวข้อฟอร์ม -->
    <div class="card-header">
      <i class="fas fa-key"></i>
      <h2>ตั้งรหัสผ่านใหม่</h2>
    </div>

    <!-- ฟอร์ม -->
    <div class="card-body">
      <% if (locals.success) { %>
        <div class="alert alert-success">
          <i class="fas fa-check-circle"></i>
          <%- success %>
        </div>
      <% } %>
      <% if (locals.error) { %>
        <div class="alert alert-danger">
          <i class="fas fa-exclamation-circle"></i>
          <%- error %>
        </div>
      <% } %>

      <form method="POST">
        <input type="hidden" name="token" value="<%= locals.token || '' %>">

        <div class="form-group">
          <label for="password">รหัสผ่านใหม่ (อย่างน้อย 8 ตัว)</label>
          <input type="password" id="password" name="password" minlength="8" required>
        </div>

        <div class="form-group">
          <label for="confirm_password">ยืนยันรหัสผ่าน</label>
          <input type="password" id="confirm_password" name="confirm_password" minlength="8" required>
        </div>

        <button type="submit" class="btn-primary">
          <i class="fas fa-check-circle"></i> บันทึกรหัสผ่านใหม่
        </button>
      </form>

      <div class="back-link">
        <a href="/login">
          <i class="fas fa-arrow-left"></i> กลับไปหน้าเข้าสู่ระบบ
        </a>
      </div>
    </div>
  </div>
</body>
</html>