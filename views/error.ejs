<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ข้อผิดพลาด | ระบบจัดเก็บเกียรติบัตร</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { 
      background: linear-gradient(135deg, #f5f7ff 0%, #e6f2ff 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
    }
    .error-container {
      max-width: 600px;
      margin: 0 auto;
      text-align: center;
      padding: 2rem;
    }
    .error-icon {
      font-size: 4rem;
      margin-bottom: 1.5rem;
      color: #ef4444;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-icon">⚠️</div>
    <h1 class="text-danger">🚫 เกิดข้อผิดพลาด</h1>
    
    <% if (locals.error && typeof locals.error === 'object' && locals.error.message) { %>
      <p class="lead text-muted"><%= locals.error.message %></p>
    <% } else if (locals.message) { %>
      <p class="lead text-muted"><%= locals.message %></p>
    <% } else { %>
      <p class="lead text-muted">ไม่สามารถประมวลผลคำขอได้</p>
    <% } %>

    <div class="mt-4">
      <a href="/" class="btn btn-primary">กลับหน้าหลัก</a>
      <% if (locals.user) { %>
        <a href="/teacher" class="btn btn-outline-primary">กลับแดชบอร์ด</a>
      <% } %>
    </div>
  </div>
</body>
</html>