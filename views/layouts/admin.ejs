<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= locals.title || 'ระบบ E-Certificate สพป.ขอนแก่น เขต 2' %></title>

  <!-- Use system font stack (avoid external Google Fonts) -->

  <!-- 📦 Bootstrap CSS (แก้ช่องว่าง) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- 🎨 Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, "Noto Sans Thai", 'Noto Sans', sans-serif;
      background-color: #f1f5f9;
      display: flex;
      min-height: 100vh;
      margin: 0;
      flex-direction: column;
    }

    /* Navbar */
    .navbar {
      background: linear-gradient(90deg, #125be3, #1745ef);
      color: white;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .navbar-brand, .navbar-nav .nav-link {
      color: white !important;
    }
    .navbar-brand {
      font-weight: 600;
      letter-spacing: 0.5px;
    }
    .btn-outline-light {
      border-color: rgba(255,255,255,0.7);
      color: white;
      transition: all 0.3s ease;
    }
    .btn-outline-light:hover {
      background-color: white;
      color: #1854d7;
      border-color: white;
    }

    /* Sidebar */
    .sidebar {
      background-color: #1062c0;
      color: white;
      width: 230px;
      position: fixed;
      top: 56px;
      left: 0;
      height: calc(100vh - 56px);
      padding-top: 20px;
      overflow-y: auto;
      box-shadow: 2px 0 12px rgba(0,0,0,0.1);
      border-right: 1px solid rgba(255,255,255,0.1);
    }
    .sidebar a {
      display: block;
      padding: 12px 20px;
      color: white;
      text-decoration: none;
      font-weight: 500;
      border-radius: 0 20px 20px 0;
      transition: 0.3s;
      margin: 4px 0;
    }
    .sidebar a:hover, .sidebar a.active {
      background: #143ab8;
      box-shadow: inset 0 0 10px rgba(0,0,0,0.1);
      border-left: 4px solid #fde047;
    }

    /* Main content */
    main {
      margin-left: 230px;
      padding: 2rem;
      flex: 1;
      background-color: #f1f5f9;
      min-height: calc(100vh - 56px);
    }

    /* Cards */
    .card {
      border-radius: 12px;
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 10px 0;
      color: #6b7280;
      font-size: 0.9rem;
      background: #fff;
      box-shadow: 0 -2px 6px rgba(0,0,0,0.05);
      margin-top: auto;
    }

    /* Scrollbar */
    .sidebar::-webkit-scrollbar {
      width: 6px;
    }
    .sidebar::-webkit-scrollbar-thumb {
      background-color: rgba(255,255,255,0.3);
      border-radius: 3px;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .sidebar {
        position: relative;
        width: 100%;
        height: auto;
        top: 0;
        box-shadow: none;
        border-right: none;
      }
      main {
        margin-left: 0;
        padding: 1rem;
      }
      .navbar-brand {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>

  <!-- 🟩 Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand ms-3" href="/admin/dashboard">
        <i class="bi bi-mortarboard"></i> สพป.ขอนแก่น เขต 2
      </a>
      <div class="d-flex align-items-center me-3">
        <% if (locals.user && locals.user.full_name) { %>
          <span class="text-white me-2 d-none d-sm-inline">
            <i class="bi bi-person-circle"></i> <%= locals.user.full_name %>
          </span>
        <% } %>
        <a href="/logout" class="btn btn-light btn-sm">
          <i class="bi bi-box-arrow-right me-1"></i> ออกจากระบบ
        </a>
      </div>
    </div>
  </nav>

  <!-- 📚 Sidebar -->
  <aside class="sidebar">
    <a href="/admin/dashboard" class="<%= (locals.activePage === 'dashboard') ? 'active' : '' %>">
      <i class="bi bi-speedometer2 me-2"></i> แดชบอร์ด
    </a>
    <a href="/admin/teachers" class="<%= (locals.activePage === 'teachers') ? 'active' : '' %>">
      <i class="bi bi-person-fill me-2"></i> จัดการครู
    </a>
    <a href="/admin/staffs" class="<%= (locals.activePage === 'staffs') ? 'active' : '' %>">
      <i class="bi bi-people-fill me-2"></i> จัดการบุคลากร
    </a>
    <a href="/admin/certificate-requests" class="<%= (locals.activePage === 'certificate-requests') ? 'active' : '' %>">
      <i class="fas fa-tasks"></i> จัดการคำขอ
    </a>
    <a href="/admin/certificates" class="<%= (locals.activePage === 'certificates') ? 'active' : '' %>">
      <i class="bi bi-award me-2"></i> เกียรติบัตรทั้งหมด
    </a>
    <a href="/admin/districts" class="<%= (locals.activePage === 'districts') ? 'active' : '' %>">
      <i class="bi bi-geo-alt-fill me-2"></i> อำเภอ
    </a>
          <!-- ✅ แก้แล้ว: เปลี่ยนจาก /teacher/guide → /guide -->
          <a href="/guide" class="<%= (locals.activePage === 'guide') ? 'active' : '' %>">
            <i class="fas fa-book"></i> คู่มือ
          </a>
  </aside>

  <!-- 📄 Main Content -->
  <main>
    <% if (locals.success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= locals.success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    <% if (locals.error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= locals.error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <%- body %>
  </main>

  <!-- 📌 Footer -->
  <footer>
    © <%= new Date().getFullYear() %> สำนักงานเขตพื้นที่การศึกษาประถมศึกษาขอนแก่น เขต 2
  </footer>

  <!-- 💡 Bootstrap JS (แก้ช่องว่าง) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>