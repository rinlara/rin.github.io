<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "ร สพป.ขอนแก่น เขต 2" %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Use system font stack instead of external Google Fonts -->

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, "Noto Sans Thai", 'Noto Sans', sans-serif;
      background-color: #f4f6fa;
      color: #1e293b;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header.navbar {
      background: linear-gradient(90deg, #00308f, #0056d2);
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15);
      border-bottom: 4px solid #ffcc00;
      padding: 0.6rem 0;
    }

    .navbar-brand {
      display: flex;
      align-items: center;
      font-weight: 600;
      color: white !important;
      font-size: 1.15rem;
    }

    .navbar-brand img {
      height: 52px;
      width: 52px;
      margin-right: 12px;
      border-radius: 50%;
      background: white;
      border: 2px solid #ffcc00;
    }

    .navbar-nav .nav-link {
      color: white !important;
      font-weight: 500;
      border-radius: 8px;
      transition: 0.3s;
    }

    .navbar-nav .nav-link:hover {
      background: rgba(255, 255, 255, 0.15);
      color: #ffdd33 !important;
    }

    main.container {
      flex: 1;
      margin-top: 60px;
      margin-bottom: 40px;
      background: white;
      border-radius: 16px;
      padding: 2rem;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
    }

    footer {
      background: linear-gradient(90deg, #002b80, #004bbd);
      color: white;
      text-align: center;
      padding: 18px 0;
      font-size: 0.95rem;
      margin-top: auto;
    }
  </style>
</head>

<body>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navMenu">
        <ul class="navbar-nav ms-auto align-items-lg-center">
         
        </ul>
      </div>
    </div>
  </header>

  <main class="container">
    <%- body %>
  </main>

  <footer>
    <p>© 2025 สพป.ขอนแก่น เขต 2 | พัฒนาโดยฝ่ายเทคโนโลยีสารสนเทศ</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
