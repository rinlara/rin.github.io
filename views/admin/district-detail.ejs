<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "รายละเอียดอำเภอ" %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <!-- Google Fonts: Sarabun -->
  <!-- Removed Google Fonts to avoid CSP/font loading issues -->
  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      background-color: #f8f9fa;
    }
    .gov-header {
      background-color: #0d2c54;
      color: white;
      border-bottom: 4px solid #d4af37;
      padding: 1.5rem 0;
      margin-bottom: 2rem;
    }
    .gov-card {
      border: 1px solid #ced4da;
      border-radius: 6px;
      box-shadow: none;
      background-color: white;
    }
    .stat-card {
      border: 1px solid #ced4da;
      border-radius: 6px;
      background-color: white;
      padding: 1.25rem;
      text-align: center;
    }
    .btn-back {
      background-color: #0d2c54;
      border-color: #0d2c54;
      color: white;
      font-weight: 500;
    }
    .btn-back:hover {
      background-color: #123b70;
      border-color: #123b70;
    }
    .table thead th {
      background-color: #e9ecef;
      font-weight: 600;
      color: #333;
    }
    .table-hover tbody tr:hover {
      background-color: #f8f9fa;
    }
  </style>
</head>
<body>
  <div class="container py-3">
    <!-- 🔹 ส่วนหัว — แบบราชการ -->
    <section class="gov-header">
      <div class="container">
        <div class="d-flex justify-content-between align-items-center">
          <h2 class="mb-0 fs-4 fw-bold">
            <i class="bi bi-geo-alt-fill me-2"></i> อำเภอ <%= district %>
          </h2>
          <a href="/admin/districts" class="btn btn-back btn-sm">
            <i class="bi bi-arrow-left me-1"></i> กลับหน้าอำเภอ
          </a>
        </div>
      </div>
    </section>

    <!-- 📊 สถิติ -->
    <div class="row g-4 mb-5">
      <div class="col-md-6">
        <div class="stat-card">
          <div class="text-primary mb-2">
            <i class="bi bi-building fs-2"></i>
          </div>
          <h6 class="text-muted mb-1">จำนวนโรงเรียน</h6>
          <h4 class="fw-bold text-primary mb-0"><%= totalSchools %></h4>
        </div>
      </div>
      <div class="col-md-6">
        <div class="stat-card">
          <div class="text-success mb-2">
            <i class="bi bi-person-lines-fill fs-2"></i>
          </div>
          <h6 class="text-muted mb-1">จำนวนครู</h6>
          <h4 class="fw-bold text-success mb-0"><%= totalUsers %></h4>
        </div>
      </div>
    </div>

    <!-- 🏫 รายการโรงเรียน -->
    <div class="card gov-card mb-4">
      <div class="card-header px-3 py-2" style="background-color: #f8f9fa; border-bottom: 2px solid #0d2c54;">
        <h5 class="mb-0 fw-bold"><i class="bi bi-building me-2"></i> รายชื่อโรงเรียน</h5>
      </div>
      <div class="card-body p-0">
        <% if (schools && schools.length > 0) { %>
          <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
              <thead>
                <tr>
                  <th class="col-8 px-3 py-2">ชื่อโรงเรียน</th>
                  <th class="col-4 px-3 py-2 text-center">จำนวนครู</th>
                </tr>
              </thead>
              <tbody>
                <% schools.forEach(school => { %>
                  <tr>
                    <td class="px-3 py-2"><%= school.school_name %></td>
                    <td class="px-3 py-2 text-center"><%= school.total_count || 0 %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <p class="text-muted text-center py-3 mb-0">ไม่มีข้อมูลโรงเรียนในอำเภอนี้</p>
        <% } %>
      </div>
    </div>

    <!-- 👩‍🏫 รายชื่อครู -->
    <div class="card gov-card">
      <div class="card-header px-3 py-2" style="background-color: #f8f9fa; border-bottom: 2px solid #0d2c54;">
        <h5 class="mb-0 fw-bold"><i class="bi bi-people-fill me-2"></i> รายชื่อครู</h5>
      </div>
      <div class="card-body p-0">
        <% if (users && users.length > 0) { %>
          <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
              <thead>
                <tr>
                  <th class="px-3 py-2">ชื่อ-สกุล</th>
                  <th class="px-3 py-2">โรงเรียน</th>
                  <th class="px-3 py-2">ตำแหน่ง</th>
                  <th class="px-3 py-2">วิชา</th>
                  <th class="px-3 py-2">อีเมล</th>
                  <th class="px-3 py-2">เบอร์โทร</th>
                </tr>
              </thead>
              <tbody>
                <% users.forEach(user => { %>
                  <tr>
                    <td class="px-3 py-2"><%= user.full_name %></td>
                    <td class="px-3 py-2"><%= user.school_name || '-' %></td>
                    <td class="px-3 py-2"><%= user.position || '-' %></td>
                    <td class="px-3 py-2"><%= user.subject || '-' %></td>
                    <td class="px-3 py-2"><%= user.email || '-' %></td>
                    <td class="px-3 py-2"><%= user.phone || '-' %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <p class="text-muted text-center py-3 mb-0">ไม่มีข้อมูลครูในอำเภอนี้</p>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>